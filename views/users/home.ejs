<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home</title>
    <%- include('../cdn_link.ejs')%>
    <style>
      body {
        background-image: linear-gradient(#b33e8f, #324ab2);
        height: 120vh;
      }
    </style>
  </head>
  <body>
    <!-- navbar -->
    <nav class="navbar navbar-expand-lg px-5 pt-3">
      <a class="navbar-brand fw-bold" href="#" style="color: #59c06d">Navbar</a>
      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link active" href="#" style="color: #59c06d">Home </a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" style="color: #59c06d">About</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" style="color: #59c06d">Service</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#" style="color: #59c06d">Contact</a>
          </li>
        </ul>

        <a href="/admin" class="ms-auto"><button class="btn btn-light">Admin</button></a>
      </div>
    </nav>

    <!-- card -->
    <div class="row m-0 p-5">
        <% for(let i=0;i<data.length;i++){%>
            <div class="col-md-3 col-sm-6 col-12" style="height: 50vh;"> 
              <div class="card" style="height: 80vh;">
                <img class="card-img-top" height="60%" src="uploads/<%= data[i].card_img%>" alt="Card image cap">
                <div class="card-body p-3">
                  <h5 class="card-title"><%= data[i].card_title%></h5>
                  <p class="card-text"><%= data[i].card_caption%></p>
                  <a href="#" class="btn btn-primary">Buy Now</a>
                </div>
               </div>
            </div>
        <%}%>
    </div>
  </body>
</html>
